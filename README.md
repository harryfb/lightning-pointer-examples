# Lightning Pointer Examples

This project provides a number of examples, illustrating the use of the Lightning pointer API in practice.

## Running

From the root directory, run

```
npm install && node index.js
```
